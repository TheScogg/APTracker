/*
  AP Tracker D1 schema reference.

  The executable schema now lives in:
    /Users/chris/APTracker/migrations/0001_d1_core.sql

  Keep this file as the stable documentation entrypoint so existing notes and
  references do not break, but treat the migration file as the source of truth.
*/

-- See ../migrations/0001_d1_core.sql for the current Cloudflare D1 schema.
